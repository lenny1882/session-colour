# Preview output from tools/
*.svg
# Editor and OS noise
*.swp
*~
.DS_Store
# Local backups made while editing
*.bak
*.bak-*

# Sandbox bookkeeping created in the repo root by the Claude Code sandbox.
# Not part of this project — but .claude/skills/ holds real project skills
# (e.g. the release process) and is tracked deliberately.
.claude/*
!.claude/skills/
.mcp.json
